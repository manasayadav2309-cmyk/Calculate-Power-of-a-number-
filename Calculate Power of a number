#include <stdio.h>
int main() {
    int base, exp, i;
    long power = 1;
    scanf("%d %d", &base, &exp);

    for(i = 1; i <= exp; i++)
        power = power * base;

    printf("Result = %ld", power);
    return 0;
}
